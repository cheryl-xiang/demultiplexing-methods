name: demux-r
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.4
  - r-devtools
  - r-tidyverse
  - r-seurat
  - bioconductor-shortread

  # installed via install.packages: BiocManager
  # installed via BiocManager: DropletUtils
