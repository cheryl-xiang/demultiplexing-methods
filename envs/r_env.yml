name: demux-r
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-base=4.4
  - r-devtools
  - r-tidyverse
  - bioconductor-shortread
  - r-dotcall64
  - r-spam
  - hdf5
  - r-hdf5r

  # installed via BiocManager
  # BiocManager itself: install.packages('BiocManager', repos='https://cran.r-project.org')
  # BiocManager::install('edgeR')
  # BiocManager::install('DropletUtils')
  # BiocManager::install("demuxmix")

  # installed via devtools::install_github
  # devtools::install_github('Gartner-Lab/deMULTIplex2')
  # devtools::install_github(repo = 'bimberlab/cellhashR', ref = 'master', dependencies = TRUE, upgrade = 'always')

  # installed via install.packages
  # install.packages(c('Seurat', 'SeuratObject', 'Rtsne', 'sctransform'), repos='https://cran.r-project.org', dependencies=TRUE)